repositories {
    maven {
        url 'https://artifactory.workday.com/artifactory/virtrelease'
        credentials {
            username = AF_USER
            password = AF_PASSWORD
        }
    }

    maven {
        url "https://artifactory.workday.com/artifactory/yet-another-gradle-repos/"
        credentials {
            username = AF_USER
            password = AF_PASSWORD
        }
    }

    maven {
        url "https://artifactory.workday.com/artifactory/linkedin.jfrog.io/open-source"
        credentials {
            username = AF_USER
            password = AF_PASSWORD
        }
    }

    maven {
        url "https://artifactory.workday.com/artifactory/packages.confluent.io/maven"
        credentials {
            username = AF_USER
            password = AF_PASSWORD
        }
    }
}

try {
    subprojects {
        project.repositories.addAll(rootProject.repositories)
    }
} catch (Throwable t) {
}
